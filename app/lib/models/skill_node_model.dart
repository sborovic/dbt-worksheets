class SkillNode {
  int id;
  String title;
  String description;
  bool isLeaf;

  SkillNode(
      {required this.id,
      required this.title,
      required this.description,
      required this.isLeaf});
}
