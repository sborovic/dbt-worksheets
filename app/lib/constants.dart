import 'package:flutter/material.dart';

final Map<String, Locale> locales = {
  'en': const Locale.fromSubtags(languageCode: 'en'),
  'sr-Latn': const Locale.fromSubtags(languageCode: 'sr', scriptCode: 'Latn'),
};
